name: Replace
on:
  push:
    paths:
      - ".github/workflows/replace.yml"
jobs:
  replace-text:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Replace text
        shell: bash
        run: |
          declare -A replacements=(
            ["auth_1"]="MAX_AUTHORIZED_USERS"
            ["auth_2"]="AUTHORIZED_USER_ADDED"
            ["auth_3"]="USER_ALREADY_AUTHORIZED"
            ["auth_4"]="AUTHORIZED_USER_REMOVED"
            ["auth_5"]="USER_NOT_AUTHORIZED"
            ["auth_6"]="FETCHING_AUTHORIZED_USERS"
            ["auth_7"]="AUTHORIZED_USERS_LIST"
            ["auth_8"]="AUTHORIZED_BY"
          )



          for key in "${!replacements[@]}"; do
            find . -type d \( -name ".git" -o -path "./.github/*" \) -prune -o \
            \( -type f \( -name "*.py" -o -name "*.yml" \) \) \
            -exec sed -i "s/\b$key\b/${replacements[$key]}/g" {} +
          done

      - name: Commit Changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Replace text in Python files"
          commit_options: "--no-verify"
          repository: .
          commit_user_name: "github-actions[bot]"
          commit_user_email: "41898282+github-actions[bot]@users.noreply.github.com"
          commit_author: "github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>"

      - name: Delete old workflow run
        uses: Viyomx/delete-workflow-runs@dev
